apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: swlabpods
spec:
  template:
    spec:
      containers:
        - name: controller
          env:
            - name: K8S_NAMESPACE
              value: kda-test
